{
  "name": "guardrails-test-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json",
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 4000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
